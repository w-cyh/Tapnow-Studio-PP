# Debug13 现场记录（Storyboard，2026-02-11）

## A. 现象

- 镜头 A 导入或删除参考图时，镜头 B/C 同步变化。
- 小图删除触发整镜头输入清空。
- 小图缺少可直接操作的预览入口。

## B. 快速定位结论

1. `isSameShotId` 误将 `shot-时间戳-序号` 视为同一镜头，导致 `updateShot` 扩散更新。
2. 主图删除分支过于粗暴，未按“当前项删除”建模。
3. 小图操作缺少独立控制按钮，交互路径不完整。

## C. 已执行修复

1. 收敛镜头 ID 匹配策略为“严格 ID + 纯数字兼容”。
2. 主图删除改为优先提升下一张参考图，而非直接全清空。
3. 小图新增预览按钮、删除按钮、双击预览。
4. 多图上传时补齐 `referenceImages` 和 `image_url` 一致性写回。

## D. 验证摘要

- 单镜头操作隔离：通过。
- 跨镜头串写：未复现。
- 小图预览/删除：通过。
- 构建与 smoke：通过。
